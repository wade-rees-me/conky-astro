--[[
#=====================================================================================
# Date	: package-date
# Author  : Nicola Bicocchi
# Version : package-version
# License : Distributed under the terms of GNU GPL version 2 or later
#======================================================================================
# CONKY
# For commands in conky.config section:
# http://conky.sourceforge.net/config_settings.html
#
# For commands in conky.text section:
# http://conky.sourceforge.net/variables.html
#
# A PDF with all variables is provided
#=====================================================================================
# FONTS
# To avoid copyright infringements you will have to download
# and install the fonts yourself sometimes.
#=====================================================================================
# GENERAL INFO ABOUT FONTS
# Go and look for a nice font on sites like http://www.dafont.com/
# Download and unzip - double click the font to install it (font-manager must be installed)
# No font-manager then put fonts in ~/.fonts
# Change the font name in the conky
# The name can be known with a command in the terminal: fc-list | grep "part of name"
# Change width and height of the conky according to font
# Reboot your system or fc-cache -fv in terminal
# Enjoy
#=====================================================================================
]]

conky.config = {
	--Various settings

	background = true, 							-- forked to background
	cpu_avg_samples = 2,						-- The number of samples to average for CPU monitoring.
	diskio_avg_samples = 10,					-- The number of samples to average for disk I/O monitoring.
	double_buffer = true,						-- Use the Xdbe extension? (eliminates flicker)
	if_up_strictness = 'address',				-- how strict if testing interface is up - up, link or address
	net_avg_samples = 2,						-- The number of samples to average for net data
	no_buffers = true,							-- Subtract (file system) buffers from used memory?
	temperature_unit = 'celsius',				-- fahrenheit or celsius
	text_buffer_size = 2048,					-- size of buffer for display of content of large variables - default 256
	update_interval = 2,						-- update interval
	imlib_cache_size = 0,						-- disable image cache to get a new spotify cover per song


	--Windows

	own_window = true,							-- create your own window to draw
	own_window_argb_value = 100,				-- real transparency - composite manager required 0-255
	own_window_argb_visual = true,				-- use ARGB - composite manager required
	own_window_colour = '#000000',				-- set colour if own_window_transparent no
	own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',  -- if own_window true - just hints - own_window_type sets it
	own_window_transparent = false,				-- if own_window_argb_visual is true sets background opacity 0%
	own_window_title = 'system_conky',			-- set the name manually  - default conky "hostname"
	own_window_type = 'desktop',				-- if own_window true options are: normal/override/dock/desktop/panel


	--Placement

	alignment = 'top_left',						-- top_left,top_middle,top_right,bottom_left,bottom_middle,bottom_right,
	gap_x = 3105,								-- pixels between right or left border 15
	gap_y = 30,									-- pixels between bottom or left border 0
	minimum_height = 1480,						-- minimum height of window
	minimum_width = 800,						-- minimum width of window
	maximum_width = 800,						-- maximum width of window


	--Graphical

	border_inner_margin = 10, 					-- margin between border and text
	border_outer_margin = 10, 					-- margin between border and edge of window
	border_width = 1, 							-- border width in pixels
	default_bar_width = 0,						-- default is 0 - full width
	default_bar_height = 25,					-- default is 6
	default_gauge_height = 25,					-- default is 25
	default_gauge_width = 0,					-- default is 40
	default_graph_height = 80,					-- default is 25
	default_graph_width = 0,					-- default is 0 - full width
	default_shade_color = '#000000',			-- default shading colour
	default_outline_color = '#000000',			-- default outline colour
	draw_borders = true,						-- draw borders around text
	draw_graph_borders = true,					-- draw borders around graphs
	draw_shades = false,						-- draw shades
	draw_outline = false,						-- draw outline
	stippled_borders = 0,						-- dashing the border


	-- Textual

	extra_newline = false,						-- extra newline at the end - for asesome's wiboxes
	format_human_readable = true,				-- KiB, MiB rather then number of bytes
	max_text_width = 0,							-- 0 will make sure line does not get broken if width too smal
	max_user_text = 16384,						-- max text in conky default 16384
	override_utf8_locale = true,				-- force UTF8 requires xft
	short_units = true,							-- shorten units from KiB to k
	top_name_width = 21,						-- width for $top name value default 15
	top_name_verbose = false,					-- If true, top name shows the full command line of  each  process - Default value is false.
	uppercase = false,							-- uppercase or not
	use_spacer = 'none',						-- adds spaces around certain objects to align - default none
	use_xft = true,								-- xft font - anti-aliased font
	xftalpha = 1,								-- alpha of the xft font - between 0-1


	-- Fonts

	font  = 'Ubuntu Mono:size=11',  			-- font
	font0 = 'Ubuntu Mono:bold:size=4',	 		-- font
	font1 = 'Ubuntu Mono:bold:size=8',  		-- font
	font2 = 'Ubuntu Mono:bold:size=10',  		-- font
	font3 = 'Ubuntu Mono:bold:size=11',  		-- font
	font4 = 'Ubuntu Mono:bold:size=12',  		-- font
	font5 = 'Ubuntu Mono:bold:size=14',  		-- font
	font6 = 'Ubuntu Mono:bold:size=16',  		-- font
	font7 = 'Ubuntu Mono:bold:size=20',  		-- font
	font8 = 'Ubuntu Mono:bold:size=24',  		-- font

	-- Colors

	-- #FFFFFF									-- white
	-- #000000									-- black
	-- #FF0000									-- red
	-- #00FF00									-- green
	-- #0000FF									-- blue
	-- #FFFF00									-- yellow
	-- #FFA500									-- orange
	-- #A52A2A									-- brown
	-- #808080									-- gray
	-- #D3D3D3									-- lightgrey
	-- #A9A9A9									-- darkgrey
	-- #00FFFF									-- cyan
	-- #FF00FF									-- magenta
	-- #EE82EE									-- violet
	-- #FFD700									-- gold
	-- #FFC0CB									-- pink

	default_color = '#FFFFFF',					-- White
	color0 = '#A569BD',							-- Lavender Purple
	color1 = '#FF5733',							-- Red-Orange
	color2 = '#33C1FF',							-- Sky Blue
	color3 = '#00FF00',							-- Green
	color4 = '#FFC300',							-- Yellow-Gold
	color5 = '#8E44AD',							-- Purple
	color6 = '#E67E22',							-- Orange
	color7 = '#3498DB',							-- Blue
	color8 = '#FF0000',							-- Dark Red
	color9 = '#1ABC9C',							-- Teal
};


conky.text = [[
	#
	# Time
	#
	${goto 30}${color white}${font8}${alignc}${time %A %d %B %Y} - ${time %H:%M} (${time %I:%M %p})${font}
	${execpi 28800 /home/wade/Conky/conky-python/bin/sun.py}
	#${execpi 6 /home/wade/Conky/conky-python/bin/warning.py}
	#
	# Weather - rotating
	#
	${execpi 180 /home/wade/Conky/conky-python/bin/airports.py}

	#
	# System
	#
	${execpi 1800 /home/wade/Conky/conky-python/bin/system.py}
	#
	# Network
	#
	${execpi 60 /home/wade/Conky/conky-python/bin/network.py}
	#
	# Memory
	#
	${execpi 60 /home/wade/Conky/conky-python/bin/memory.py}
	#
	# GPU
	#
	${execpi 3 /home/wade/Conky/conky-python/bin/gpu.py}
	#
	# CPU
	#
	${execpi 3 /home/wade/Conky/conky-python/bin/cpu.py}
	#
	# Disks
	#
	${execpi 60 /home/wade/Conky/conky-python/bin/disks.py}

	#
	# Planets, Stars and Constellations
	#
	${execpi 1800 /home/wade/Conky/conky-python/bin/planets.py}
	${execpi 60 /home/wade/Conky/conky-python/bin/exoplanets.py}
	${execpi 60 /home/wade/Conky/conky-python/bin/stars.py}

	${execpi 28800 /home/wade/Conky/conky-python/bin/definitions.py}
]];

